# Copy to .env.local for local development overrides

# Backend API base URL (default: http://localhost:8080, proxied via /api)
# VITE_API_BASE_URL=http://localhost:8080

# Override proxy target
# VITE_PROXY_TARGET=http://my-custom-host:8080
